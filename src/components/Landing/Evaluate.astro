---
import Title from "./Title";
import { Star, User } from "@icon-park/react";
---

<section>
  <Title
    title="全网客户的真实声音"
    desc="致力服务每一个客户，成交只是服务的开始，我们希望您可以一直使用我们的最新产品，当然不管是什么产品！"
    client:only="react"
  />
  <!-- Container -->
  <div class="mx-auto w-full max-w-7xl px-5 py-2 md:px-10 ">
    <!-- Testimonial List -->
    <ul class="mb-6 grid gap-5 sm:grid-cols-2 md:mb-16 md:grid-cols-3">
      <li
        class="grid gap-8 border border-solid border-[#dfdfdf] bg-white p-8 md:p-10"
      >
        <div class="flex">
          {[1,2,3,4,5].map(() => (
            <Star client:load theme="filled" size="24" fill="#FFD700" class="mr-1.5"/>
          ))}
        </div>
        <p class="text-[#647084]">
          我从上学的时候也就17年买的卡，之后一直在使用他们家的产品，经常给我身边的朋友推荐[流客科技]。
          从上学到上班, 这便是最深情的告白"
        </p>
        <div class="flex items-center">
          <User client:load theme="filled" size="48" fill="#666" class="mr-4"/>
          <div class="flex flex-col">
            <h6 class="font-bold">Laila Bahar</h6>
            <p class="text-sm text-[#636262]">Designer</p>
          </div>
        </div>
      </li>
      <li
        class="grid gap-8 border border-solid border-[#dfdfdf] bg-white p-8 md:p-10"
      >
        <div class="flex">
          {[1,2,3,4,5].map(() => (
            <Star client:load theme="filled" size="24" fill="#FFD700" class="mr-1.5"/>
          ))}
        </div>
        <p class="text-[#647084]">
          售后服务很好，有问题积极处理，而且卡的网速也是快的飞起，如果网速不行，可以找客户给你调换，使用起来完全无顾虑
        </p>
        <div class="flex items-center">
          <User client:load theme="filled" size="48" fill="#666" class="mr-4"/>
          <div class="flex flex-col">
            <h6 class="font-bold">Laila Bahar</h6>
            <p class="text-sm text-[#636262]">Designer</p>
          </div>
        </div>
      </li>
      <li
        class="grid gap-8 border border-solid border-[#dfdfdf] bg-white p-8 md:p-10"
      >
        <div class="flex">
          {[1,2,3,4,5].map(() => (
            <Star client:load theme="filled" size="24" fill="#FFD700" class="mr-1.5"/>
          ))}
        </div>
        <p class="text-[#647084]">
          不是宝藏，胜似宝藏，能找到一个长期更新各种卡，而且不跑路的老板太难了，我已经用户有4年了，希望老板越来越强，生意越来越好
        </p>
        <div class="flex items-center">
          <User client:load theme="filled" size="48" fill="#666" class="mr-4"/>
          <div class="flex flex-col">
            <h6 class="text-base font-bold">张非</h6>
            <p class="text-sm text-[#636262]">Designer</p>
          </div>
        </div>
      </li>
    </ul>
  </div>
</section>
